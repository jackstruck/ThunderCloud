# Historical backfill teardown — 2026-09-06

## Acceptance boundary

The user accepted historical processing and gallery backfill as complete. We did not
rerun historical corpus reconciliation, infer new final counts, or relaunch processing.
The automatic local-first transition, pilot, new-URL catch-up, and observation period
are not reported complete. Their earlier completion markings were withdrawn.

## Applied cloud changes

- Deleted `face-batch-backfill-inventory` and its two `backfill-audit/` object-user
  grants. Archived job and execution definitions before deletion.
- Deleted the five terminal `face-ctl-20260902-*` Google Cloud Batch jobs after
  preserving their definitions/status. Removed `roles/batch.jobsEditor`,
  `roles/batch.agentReporter`, and the Batch-specific `roles/batch.serviceAgent` grant.
  Disabled `batch.googleapis.com` explicitly; removing its Terraform management entry
  alone would have left the API enabled because `disable_on_destroy` was false.
- Deleted the historical-only Artifact Registry `inventory` package (nine image
  records), after archiving all image digests and metadata.
- Applied Artifact Registry retention: keep permanent deployed image-version prefixes,
  explicitly configured rollback versions, and the five newest versions per package;
  delete other tagged or untagged versions older than 30 days. Prefixes use the API's
  64-character maximum. The first policy request exceeded this limit and was rejected;
  the corrected registry-only plan was reviewed and applied successfully.
- Preserved all three permanent jobs (`face-batch-gpu-drain`, `face-interactive-gpu`,
  `face-ingest-drain`), `face-console`, both schedulers, all three operational alerts,
  runtime accounts, invocation permissions, shared networking, Cloud SQL, archive and
  gallery prefixes, repository, and encryption keys. Worker/console image pins and
  processing settings were unchanged. Shared runtime description and Cloud Run client
  metadata were updated without replacing resources or IAM grants.

The initial reviewed Terraform plan contained six deletions, three updates, and no
creates/replacements. The correction plan updated only Artifact Registry. A final
full refreshed Terraform plan returned exit code 0 and no changes.

## Local and documentation changes

- Archived superseded planning, implementation, and backfill plans, original Phase 1
  review, old commands, and completed selections in this directory.
- Archived twelve original local inventory, process-selection, execution, and gallery
  sample records under `data/history/2026-09-06-backfill/`. Preserved retired source,
  cloud inventory, execution names, digests, reviewed plans, apply logs, and SHA-256
  index there. The directory is mode 0700 and files are mode 0600, ignored by Git.
- Removed legacy Batch submission code/tests, historical inventory/cloud launcher/
  Stage 1 modules and tests, their console entry points, and `Dockerfile.inventory`.
- Retained `select_remaining.py` because the future-video workflow uses it. Retained
  bounded gallery repair, migrations, and fallback records for permanent gallery
  maintenance. These are not active historical processing. No historical Cloud Run
  executions or compute VMs were active in the teardown inventory; no local processing
  process or Docker container was running.
- Removed tracked generated bulk-download package metadata and superseded Terraform
  saved plans. Kept tools/environments, models, state/backups, the acquisition manifest,
  configuration, and keys needed by the permanent workflow.
- Consolidated current setup/commands in `OPERATIONS.md`, created `ROADMAP.md`, refreshed
  README/architecture, replaced redundant cleanup/review documents with pointers, and
  added `GET /api/runs/recent` to OpenAPI. Documented enabled enrollment/public HTTPS
  fetching, managed ingestion, run recovery, gallery publication, and operator merge/
  split support without claiming unfinished identity assignment or pilot acceptance.

## Preserved audit data and cost inventory

Five immutable CSEK audit objects under `backfill-audit/` remain as historical records
(26,934,754 bytes in total); their write grants are removed. `face-staging/` was empty.
Source objects under `videos/`, representative faces under `subject-gallery/`, and
retained `training-media/` are preserved. Original fallback decisions and historical
counts are retained as evidence; no replacement reconciliation report was invented.

Backfill-only inventory compute, Batch jobs, and inventory images are removed. The
remaining ongoing resources include Cloud SQL `face-batch-pg` (`db-f1-micro`, 10 GiB
configured disk), permanent Cloud Run jobs and console, two schedulers, shared network,
registry worker/console images, keys/secrets, logs/monitoring, and durable object storage.
No Compute Engine instances were present. This is a resource/cost-driver inventory,
not a billing export or a claimed dollar saving. Old runtime images remain available
for rollback subject to the protected retention rules.

## Validation and remaining acceptance

- Remaining Python suite: 119 passed.
- Ruff passes for worker/tests and the retained selection helper. A broader scripts
  check also exposed pre-existing executable/import-order findings in
  `scripts/verify_gpu_image.py`, outside this change.
- Terraform configuration validates; the post-apply full plan reports no drift.
- A real local CPU probe queried the live Cloud SQL gallery successfully and returned
  one candidate for one retained crop. Its extra temporary review crops were removed.
- Programmatic access to the IAP console returned HTTP 401 with available credentials.
  Browser link/upload ingestion and interactive-search smoke acceptance remains open;
  the user's authenticated browser session is required. Teardown Step 6 therefore
  remains partially complete, rather than being falsely checked off.

The gcloud CLI login was expired, but existing application credentials supported all
cloud inventory and teardown actions. No additional infrastructure privileges or IAP
bypass were introduced.

## API references

The explicit out-of-Terraform deletions used the documented
[Artifact Registry package deletion API](https://docs.cloud.google.com/artifact-registry/docs/reference/rest/v1/projects.locations.repositories.packages/delete)
and [Service Usage disable API](https://docs.cloud.google.com/service-usage/docs/reference/rest/v1/services/disable).
